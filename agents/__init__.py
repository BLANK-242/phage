# Namespace package for PHAGE agents (importable for local runners/tests).
# NOTE: `adk deploy cloud_run` ships each agent's own folder only, not this
# parent, so agents must not rely on sibling imports through this package.
