# Committed reference for agents/quote_bot/.env (which is gitignored).
# All values are non-secret. Authentication is via Application Default
# Credentials locally and the Cloud Run service account in production.
GOOGLE_GENAI_USE_VERTEXAI=1
GOOGLE_CLOUD_PROJECT=phage-dev
GOOGLE_CLOUD_LOCATION=global
PHAGE_GEMINI_MODEL=gemini-3.5-flash
PHAGE_MODEL_LOCATION=global
