# Handoff — PHAGE
> **Build-history record.** A session snapshot written mid-construction and
> left unedited. Repo state, figures and component status described here are
> superseded. `docs/writeup.md` and `README.md` are the current, audited
> statements.

Date: 2026-08-12
Session: 6
Context: other — Devpost hackathon (All Things Agentic, Google Cloud)
Session language: EN
Model: Claude Sonnet 5 (review/strategy track; session opened on Opus 5, switched mid-session — see Session notes). Build track also Sonnet 5, confirmed via Claude Code banner this session.

## Current task

Building PHAGE — an immune system for AI agent fleets — for Google Cloud's All Things Agentic Hackathon, Fortified Enterprise Fleet track (deadline 2026-08-31 17:00 PDT). This session opened by discovering the prior handoff (`2026-08-11_PHAGE.md`) was stale: the real repo, HEAD `028d2c5`, was five commits ahead of what it recorded — MARROW fleet iteration + live payload-firing with OTel trace capture, a four-agent SAIL target fleet, and a fully built SENTINEL (Gemma-first-pass/Gemini-escalation triage) all existed uncaptured. A read-only Claude Code pass then mapped SENTINEL's full output contract (file:line cited). **Work stopped immediately before writing the MACROPHAGE (containment) build brief — no MACROPHAGE code exists.** Three of five agents (MARROW, VACCINATOR, SENTINEL) are done; MACROPHAGE and ARCHIVIST remain.

## Decisions made

- **Handoffs must be regenerated from `git log`/`git status` output at write time, never from in-conversation memory.** The prior handoff undercounted progress by 5 commits, causing a wasted MARROW-fleet build brief and a 3-minute Claude Code recon cycle to discover work already done. This handoff was written directly against a fresh `git log --oneline -12`.
- **SENTINEL is intentionally standalone, not wired into MARROW's fire/capture loop.** Confirmed via `adk_agent.py:6-7` docstring ("No MARROW integration — that's Stage 3, its own commit") and `grep -n sentinel src/phage/marrow/agent.py` returning only placeholder comments (`agent.py:6, 96, 223`). Not a bug — a deferred, separate task.
- **SENTINEL delivers its verdict two ways.** The bare engine fn `triage_firing()` (`triage.py:252-260`) returns a `TriageResult` directly — pure function, no ADK. The ADK wrapper `SENTINEL(BaseAgent)` (`adk_agent.py:93-185`) additionally writes the identical result to session state under key `sentinel.verdict` via `EventActions.state_delta` (`adk_agent.py:76,184`), mirroring VACCINATOR/MARROW's convention. **MACROPHAGE should read via the session-state key**, matching the established pattern, not assume a direct return-value hookup.
- **Trace databases (`phage_traces*.db`) should be `.gitignore`d, not committed** — same precedent as untracking `vaccinate_demo_output.txt` (`ea2b642`). Proposed this session; execution not confirmed — see Next steps.

## Files & artifacts produced

Committed on the VM (`~/projects/phage`), current state relevant to the next build:

| File | Purpose | Status | Resume note | Path |
|---|---|---|---|---|
| src/phage/targets.py | SAIL target fleet registry | complete | `Target` dataclass + `FLEET`: SUPPLIER-RELAY, QUOTE-BOT, ORDER-INTAKE, STOCK-KEEPER | src/phage/targets.py |
| src/phage/marrow/agent.py | Fleet iteration + payload firing + trace capture | complete | Fires every generated payload at all 4 real target agents via InMemoryRunner, captures OTel spans to phage_traces.db (`:218-278`). SENTINEL NOT wired in — comments only at `:6,:96,:223` | src/phage/marrow/agent.py |
| src/phage/sentinel/triage.py | Triage engine — verdict logic | complete | `TriageResult` frozen dataclass (`:99-110`): target_id, archetype_id, session_id, verdict (LANDED\|DECLINED\|AMBIGUOUS\|NOT_OBSERVABLE, `:86-90`), tier (DETERMINISTIC\|GEMMA\|GEMINI, `:93-96`), reasoning, supporting_spans. Two zero-LLM pre-filters run before any model call (`:265-299`) | src/phage/sentinel/triage.py |
| src/phage/sentinel/adk_agent.py | SENTINEL ADK BaseAgent wrapper | complete | Calls `triage_firing()`, writes result to session state key `sentinel.verdict` via state_delta (`:184`); also yields human-readable summary Event | src/phage/sentinel/adk_agent.py |

## Next steps

- [ ] Confirm git status is still clean and HEAD is still `028d2c5` before starting MACROPHAGE — last confirmed at the top of this session; not reverified after the SENTINEL recon pass.
- [ ] Build MACROPHAGE (containment) — consumes SENTINEL's `sentinel.verdict` session-state output. Read-only recon first: confirm how a custom Node/BaseAgent reads a sibling's `state_delta` output mid-run at MACROPHAGE's actual invocation point (the runner-timing question resolved for MARROW→VACCINATOR in Session 4 likely holds, but confirm rather than assume — that assumption habit is what caused today's desync).
- [ ] Decide and record MACROPHAGE's concrete containment action for a LANDED/AMBIGUOUS verdict (kill session? revoke tool? log-only for the hackathon demo?) — currently undesigned, see Open questions.
- [ ] Wire SENTINEL into MARROW's fire/capture loop — decide whether this precedes or follows the MACROPHAGE build.
- [ ] Apply the `.gitignore` entry for `phage_traces*.db` (proposed, not yet confirmed executed).
- [ ] Resolve the untracked `docs/*.md` pile (10 files as of this session) — commit as build-history record, or gitignore. User's call, deferred twice now.
- [ ] Delete or archive `docs/PHAGE_cc_prompt_marrow_fleet.md` (confirmed stale/superseded this session) and check the same untracked pile for `PHAGE_cc_prompt_marrow_fleet_iteration.md`, which looks like an earlier duplicate draft of already-completed work.
- [ ] Build ARCHIVIST (signature memory + its own deterministic mutation step, per Session 4 Q3 decision) — last of the five agents, not started.
- [ ] [BLOCKED: Google review] Redeem $150 credit code — hard cutoff 2026-09-03.
- [ ] Create GitHub repo and push (still local only).

## Open questions

- **MACROPHAGE's containment action is undesigned.** What does it concretely do once it reads a LANDED or AMBIGUOUS verdict from SENTINEL — terminate the target session, revoke a specific tool, log-only for the demo, something else?
- **Is SENTINEL→MARROW wiring a prerequisite for MACROPHAGE**, or can MACROPHAGE be built standalone against recorded session-state first, mirroring how SENTINEL itself was built standalone before integration?

## Session notes

- **Environment (carry forward):** Ubuntu 24.04.4 on VMware/Win11, host `phage-dev`, user `blank`. Python 3.12 via `uv` — `uv run python`, bare `python` not aliased. ADK 2.6.3, gcloud 579.0.0. Project `phage-dev` (#680106551305), org `benlekbirwalid3-org`, Agent Engine instance `1868793184486686720`. AZERTY; respond in English.
- **Claude Code upgraded to 2.1.228 this session** (from 2.1.226) via a full uninstall/reinstall, forced by a corrupted native-binary install (stub `.exe` printing "native binary not installed"). Root cause of the reinstall failures: a wedged `npm install` (missing `-g`, running inside the repo dir) held a file lock, throwing `ENOTEMPTY` on every retry. Fixed via `kill -9` on the wedged PID + clean `npm install -g`. **If EACCES/ENOTEMPTY on claude-code recurs, check `pgrep -af "npm install"` for a wedged process before reinstalling.**
- **Model discipline still applies and held after the reinstall** (confirmed Sonnet 5 · Claude Pro on relaunch banner). Four prior Opus deaths on this repo's content this project — always confirm the model line, not the tab name, at the `>` prompt.
- **SENTINEL's two deterministic pre-filters** (`decisive_tool_names()`, `matching_tool_spans()`, `triage.py:116-134`) run before either LLM tier — most triage calls likely resolve at `Tier.DETERMINISTIC` without touching Gemma or Gemini at all. Relevant to MACROPHAGE's expected verdict distribution and to any cost/latency claims in the submission writeup.
- This session's opening problem (a stale handoff producing a redundant build brief) is now the standing process fix: **regenerate handoffs from a fresh `git log` read, not from conversational memory.** Treat `2026-08-11_PHAGE.md` as superseded by this file.

---
Resume: type "resume PHAGE" or upload this file at conversation start.
